public class sample{
	public static void main(String args[])
	{
		int age = 25;
		boolean hasVoterId=false;
		String output;
		/*output = (age >=18) ? "can vote" : "cannot vote";
		System.out.println(output); */
		/* System.out.println((age >=18) ? "can vote" : "cannot vote"); */

		/*if(age>=18){
			if(hasVoterId){
				output="can vote";
			}
			else{
				output="cannot vote";

			}
		}
		else{
			output="cannot vote";
		}
		System.out.println(output); */




		/* output = (age >=18) ? (hasVoterId ? "can vote" : "cannot vote") : "cannot vote";
		System.out.println(output); */


		/* long ifStartTime = System.currentTimeMillis();            //to see how time if and ternary take to execute
		System.out.println("if Stat Time:" +ifStartTime);

		if(age>=18){
			if(hasVoterId){
				output="can vote";
			}
			else{
				output="cannot vote";

			}
		}
		else{
			output="cannot vote";
		}
		System.out.println(output);

		long ifEndTime = System.currentTimeMillis();
		System.out.println("if End Time:" +ifEndTime);

		long TernaryStartTime = System.currentTimeMillis();
		System.out.println("if Stat Time:" +TernaryStartTime);
		output = (age >=18) ? (hasVoterId ? "can vote" : "cannot vote") : "cannot vote";
		System.out.println(output); 
		long TernaryEndTime = System.currentTimeMillis();
		System.out.println("if End Time:" +TernaryEndTime);
		*/

		








	}
}